*.xpi
.project
.settings

# Internal dev-only directories — kept in the local working tree for
# development reference (parked drafts, smoke userscripts, run logs)
# but not shipped in the public repo.
_attic/
tests/smoke/
